export enum AppBundle {
    patient = "com.cloud273.patient",
    doctor = "com.cloud273.doctor",
    clinic = "com.cloud273.clinic",
    admin = "com.cloud273.admin"
}

export enum StatusType {
    created = "created",
    accepted = "accepted",
    rejected = "rejected",
    cancelled = "cancelled",
    started = "started",
    finished = "finished"
}

export enum ClinicCertType {
    working = "working",
    other = "other"
}

export enum CurrencyUnit {
    vnd = "vnd",
    usd = "usd"
}

export enum DeviceOS {
    ios = "ios",
    android = "android",
    mac = "mac",
    window = "window",
    linux = "linux",
    chrome = "chrome",
    safari = "safari",
    firefox = "firefox",
    ie = "ie",
    edge = "edge"
}

export enum DoctorCertType {
    personal = "personal",
    working = "working",
    degree = "degree",
    other = "other"
}

export enum GenderType {
    male = "male",
    female = "female",
    unknown = "unknown"
}

export enum Language {
    vi = "vi",
    en = "en"
}

export enum MedicationType {
    highBP = "highBP",
    highCholesterol = "highCholesterol",
    pregnant = "pregnant",
    cancer = "cancer"
}

export enum MessageType {
    sms = "sms",
    email = "email",
    device = "push"
}

export enum NotifyType {
    appointmentCreated = "appointmentCreated",
    appointmentAccepted = "appointmentAccepted",
    appointmentCancelled = "appointmentCancelled",
    appointmentRejected = "appointmentRejected",
    appointmentBegun = "appointmentBegun",
    appointmentFinished = "appointmentFinished",
    updateUsernameRequest = "updateUsernameRequest"
}

export enum PackageType {
    classic = "classic",
    telemed = "telemed"
}

export enum PharmacyCertType {
    working = "working",
    other = "other"
}

export enum ReviewStatus {
    accepted = "accepted",
    approved = "approved",
    rejected = "rejected"
}

export enum Trilean {
    yes = "yes",
    no = "no",
    unknown = "unknown"
}

export enum UserType {
    patient = "patient",
    clinic = "clinic",
    doctor = "doctor",
    system = "system"
}

export enum MedicineUsedType {
    uong = "uong",
    chich = "chich",
    thoa = "thoa",
    dat = "dat"
}